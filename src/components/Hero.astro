---
import { Image } from "astro:assets";
import Logo from "@/images/iglesia.png";
---

<section class="hero min-h-[90vh]">
  <Image src={Logo} alt="Logoibf" class="logo" height={400} loading="eager" />
  <div class="container" transition:animate="fade">
    <p>Un lugar de fe, esperanza y amor por las almas</p>

    <div class="flex justify-center gap-4">
      <a
        href="/servicios"
        class="inline-block px-8 py-4 rounded-md no-underline font-bold transition ease-in-out delay-75 hover:-translate-y-1 bg-mc3 text-mc2 hover:shadow-md"
        >Nuestros Servicios</a
      >
      <a
        href="/eventos"
        class="inline-block px-8 py-4 rounded-md no-underline font-bold transition ease-in-out delay-75 hover:-translate-y-1 bg-transparent text-white ring-offset-2 ring-2 hover:shadow-md"
        >Próximos Eventos</a
      >
    </div>
  </div>
</section>

<style>
  .logo {
    filter: drop-shadow(0 0 30px rgba(255, 255, 255, 0.8));
  }
  .hero {
    background-size: cover;
    background-position: center;
    color: var(--color-background);
    /* Add these lines */
    display: flex;
    flex-direction: column; /* Align items vertically */
    justify-content: center; /* Center vertically */
    align-items: center; /* Center horizontally */
    /* --- */
    text-align: center; /* This remains useful for text within the hero */
    padding: 5rem 0; /* Consider removing or adjusting padding if needed */
    min-height: 90vh; /* Maintain minimum height */
  }

  .container {
    max-width: 800px;
    margin: 0 auto;
  }
  h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    color: white;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
  }
  p {
    font-size: 1.5rem;
    margin-bottom: 2rem;
    color: white;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
  }

  .cta-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
</style>
